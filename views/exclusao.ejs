<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Confirmar Exclusão</title>
  <style>
    .container {
      max-width: 500px;
      margin: 50px auto;
      padding: 20px;
      border: 2px solid red;
      background-color: #fff5f5;
      border-radius: 10px;
      text-align: center;
    }
    .btn-danger {
      background-color: red;
      color: white;
      padding: 10px;
      border: none;
      border-radius: 5px;
      margin-right: 10px;
    }
    .btn-cancel {
      background-color: gray;
      color: white;
      padding: 10px;
      border: none;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Tem certeza que deseja excluir?</h2>
    <p>
      Você está prestes a excluir:  
      <strong><%= item.nome || item.titulo || 'Registro' %></strong>
    </p>

    <form action="<%= action %>" method="POST">
      <input type="hidden" name="_method" value="DELETE">
      <button type="submit" class="btn-danger">Sim, excluir</button>
      <a href="<%= voltar %>" class="btn-cancel">Cancelar</a>
    </form>
  </div>
</body>
</html>
